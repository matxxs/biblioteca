import { NotFound } from "@/components/pages_error";

export default function NotFoundPage() {
  return (
    <NotFound description="A página que você está procurando pode ter sido movida, removida ou não existe no nosso sistema de biblioteca." />
  );
}
